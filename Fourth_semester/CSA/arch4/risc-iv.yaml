name: "assert is_binary_palindrome(4026531855) == 1"
limit: 1000
memory_size: 0x1000
memory_mapped_io:
  0x80: [4026531855]
  0x84: []
reports:
  - name: Step-by-step log
    slice: all
    view: |
      {pc}:	{instruction}	{pc:label}
  - name: Check results
    slice: last
    view: |
      numio[0x80]: {io:0x80:dec}
      numio[0x84]: {io:0x84:dec}
    assert: |
      numio[0x80]: [] >>> []
      numio[0x84]: [] >>> [1]